version: '3.7'

services:
  db:
    image: postgres
    restart: "no"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: tracker
      POSTGRES_USER: db_user
      POSTGRES_PASSWORD: db_password

  adminer:
    image: adminer
    restart: "no"
    ports:
      - "8081:8080"
